export enum Direction {
  Up = 'up',
  Down = 'down',
  Left = 'left',
  Right = 'right',
}

export enum MessageStatus {
  Error = 'error',
  Warning = 'warning',
  OK = 'ok',
}

export enum GamePhase {
  Battling = 'battling',
  GameOver = 'game over',
  PreGame = 'pre game',
}

export enum CellState {
  Hit = 'hit',
  NotHit = 'not hit',
}
